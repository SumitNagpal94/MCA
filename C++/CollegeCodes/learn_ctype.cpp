#include<ctype.h>
#include<iostream>
using namespace std;
//zero if not alpha 
int main()
{
	char a;
	cin>>a;
	char b='A';
	cout<<isdigit(a);
	//if(isdigit(a)==0)
	//cout<<"Alphabet Found";
	//else
	//cout<<"Digit";
}
